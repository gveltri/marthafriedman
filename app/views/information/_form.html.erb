<%= form_for(@information) do |f| %>
  <% if @information.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@information.errors.count, "error") %> prohibited this information from being saved:</h2>

      <ul>
      <% @information.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
    <%= f.label :version %><br>
    <%= f.text_field :version %>
    </div>


    <div class="field">
    <%= f.label :about %><br>
    <%= f.text_area :about %>
    </div>

    <div class="field">
    <%= f.label :press %><br>
    <%= f.text_area :press %>
    </div>


    <div class="field">
	    <%= f.label :document %><br>
	    <%= f.file_field :document %>
    </div>
    <br>
    <div class="field">
	    <%= f.fields_for :press_documents do |c| %>
		    <%= c.label :pdf %><br>
		    <%= c.file_field :pdf %><br>
		    <%= c.text_field :name %>

	    <% end %>
    </div>
    <br>

  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
